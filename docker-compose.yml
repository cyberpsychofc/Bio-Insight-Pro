services:
# commented to run on-premises BACKEND and FRONTEND on VERCEL, uncomment otherwise
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "5173:5173"  # Map localhost:5173 to React app port 5173
  #   volumes:
  #     - ./frontend:/app
  #     - /app/node_modules
  #   environment:
  #     - CHOKIDAR_USEPOLLING=true
  #   stdin_open: true
  #   tty: true

  cloudfare:
    container-name: tunnel
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    environment:
      -TUNNEL_TOKEN=$TUNNEL_TOKEN
    command: tunnel --no-autoupdate run
    networks:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"  # Map localhost:8000 to Django server port
    volumes:
      - ./backend:/backend
    environment:
      - DEBUG=True # Set to False in Production
    stdin_open: true
    tty: true

networks:
  backend:
    driver: bridge